import { render, screen } from '@testing-library/react'

import { Banner } from '.'

describe('<Banner />', () => {
  it('should render the heading', () => {
    //verique se o title existe renderizado
    //verique se o subtitle existe renderizado
    //verique se o imagem existe renderizado
  })
})
